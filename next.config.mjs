/** @type {import('next').NextConfig} */
const nextConfig = {
  // REPLACE: Add your image domains here if using next/image with external sources
  // images: {
  //   domains: ["your-image-cdn.com"],
  // },
};

export default nextConfig;
